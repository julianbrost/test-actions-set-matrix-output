name: Set database versions matrix
outputs:
  matrix:
    description: 'Matrix for the next job'
runs:
  using: composite
  steps:
    - shell: bash
      run: |
        {
          echo 'matrix<<EOF'
          cat ${{ github.action_path }}/matrix.json
          echo EOF
        } | tee /dev/stderr >> "$GITHUB_OUTPUT"
